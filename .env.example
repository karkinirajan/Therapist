# Root-level env for docker-compose (local Postgres only).
# Copy to .env and change the password before running `docker compose up`.
POSTGRES_USER=therapist
POSTGRES_PASSWORD=change-me-locally
POSTGRES_DB=therapist
POSTGRES_PORT=5433
