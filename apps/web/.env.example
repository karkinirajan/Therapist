# Server-only — the FastAPI origin the Next.js route handlers proxy to.
# Intentionally NOT prefixed NEXT_PUBLIC_*: it must never reach the client
# bundle. The browser never talks to this origin directly.
API_BASE_URL=http://localhost:8000
